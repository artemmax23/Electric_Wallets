version: "3.9"

services:
  db:
    image: postgres:15
    container_name: electronic_wallets_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: wallet_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  app:
    build: .
    container_name: electronic_wallets_api
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      DATABASE_URL: postgresql+asyncpg://user:password@db:5432/wallet_db
    volumes:
      - .:/app

volumes:
  postgres_data: